//Desired API

Run test program as follows :

FLAGS : -v : Verbose , -c : collection specifier

// Runs all tests in verbose mode in collections specified by -c
./testBed -v -c:COLLECTION_NAME -c:COLLECTION_NAME ... 


Type files as follows :

#include whatever you need (not eagle)

EAGLE_START

TEST(COLLECTION_NAME, TEST_NAME)
{
    blah blah

    CHECK_EQ(blah blah)

    blah blah
}

EAGLE_END